# Nmap実践ガイド（CTF・Pentest編）

## 1. Nmapとは

* Nmapでできること
* CTF・Pentestでの位置付け
* 基本構文

---

## 2. ホスト探索（Host Discovery）

* Ping Scan（`-sn`）
* Pingを無視してスキャン（`-Pn`）
* 対象ホストの指定方法

---

## 3. ポートスキャン

* TCP SYN Scan（`-sS`）
* TCP Connect Scan（`-sT`）
* UDP Scan（`-sU`）
* 全ポートスキャン（`-p-`）
* 特定ポート指定（`-p`）
* 上位1000ポート（デフォルト）

---

## 4. サービス・バージョン検出

* Version Detection（`-sV`）
* Aggressive Version Detection（`--version-all`）
* CTFでの活用方法

---

## 5. OS検出

* OS Detection（`-O`）
* 制限事項

---

## 6. デフォルトスクリプト（NSE）

* Default Script（`-sC`）
* `default`カテゴリとは
* よく取得できる情報

---

## 7. NSEスクリプトの活用

### NSEスクリプトの実行方法

NSEを実行するには、主に`--script`オプションを使用します。

| 実行方法 | コマンド例 | 説明 |
| :--- | :--- | :--- |
| **デフォルトスキャン** | `nmap -sC <target>` | 安全で信頼性の高い「default」カテゴリーのスクリプトを一括実行します。 |
| **単一スクリプト** | `nmap --script <script_name> <target>` | 指定した特定のスクリプト（例：`banner`）のみを実行します。 |
| **カテゴリー指定** | `nmap --script <category> <target>` | 指定したカテゴリー（例：`vuln`）に属する全スクリプトを実行します。 |
| **ワイルドカード** | `nmap --script "smb*" <target>` | 名前が特定の文字列で始まるスクリプト（例：SMB関連）をまとめて実行します。http、ssh など指定可能 |
| **引数の指定** | `nmap --script <name> --script-args <args>` | スクリプトの動作に必要な追加パラメータ（ドメイン名、認証情報など）を渡します。 |
 
### 主要なスクリプトカテゴリー

スクリプトはその目的や安全性によってカテゴリー分けされています。

| カテゴリー | 役割・特徴 |
| :--- | :--- |
| **auth** | ターゲットホストの認証回避や、デフォルト認証情報の利用を試みます。 |
| **brute** | HTTP、SNMP、MySQLなどのサービスに対してパスワードの推測（ブルートフォース）を行います。 |
| **discovery** | ネットワーク上の公開情報、SNMP、ディレクトリサービスなどからさらに詳細な情報を探索します。 |
| **vuln** | 既知の脆弱性（CVEなど）が存在するかどうかをチェックします。 |
| **exploit** | ターゲットの脆弱性を実際に悪用しようと試みます。 |
| **safe** | ターゲットのサービスを停止させたり、大きな負荷をかけたりしない安全なスクリプトです。 |
| **intrusive** | ターゲットのクラッシュやリソース消費を引き起こす可能性のある、ノイジーでリスクの高いスクリプトです。 |
| **default** | 安全性、速度、信頼性のバランスが取れた、標準的なスキャンで使用されるスクリプト群です。 |

### 頻用される具体的なスクリプト例

実際のペネトレーションテストでよく活用されるスクリプトの例です。

| スクリプト名 | 用途 |
| :--- | :--- |
| **banner** | 開いているTCPポートに接続し、サービスが返してくるバナー情報を表示します。 |
| **http-enum** | Webサーバー上の隠しディレクトリや既知のアプリケーションパスを列挙します。 |
| **vulners** | 検出されたサービスバージョンに基づき、関連するCVE番号やCVSSスコア、公開Exploitへのリンクを表示します。 |
| **smb-os-discovery** | SMBプロトコルを通じて、ターゲットのOS詳細、コンピュータ名、ドメイン名などを特定します。 |
| **dns-zone-transfer** | 指定したドメインのDNSサーバーからゾーン転送（AXFR）を試み、ホスト情報を一括取得します。 |
| **http-title** | Webサーバーの各ページからHTMLの`<title>`タグの内容を抽出します。 |
| **ms-sql-info** | MS SQLサーバーのインスタンス名、バージョン、名前付きパイプなどの詳細情報を取得します。 |

### スクリプトの管理とヘルプ

スクリプトを安全かつ最新の状態で利用するための管理方法です。

| アクション | コマンド・場所 | 説明 |
| :--- | :--- | :--- |
| **情報の確認** | `nmap --script-help <script_name>` | スクリプトの概要、カテゴリー、使用例、詳細ドキュメントのURLを表示します。 |
| **DBの更新** | `nmap --script-updatedb` | 新しく追加したスクリプトをNmapに認識させるために、スクリプトデータベースを更新します。 |
| **保存ディレクトリ** | `/usr/share/nmap/scripts/` | Linux（Kali Linuxなど）においてNSEスクリプト（`.nse`ファイル）が格納されている標準的なパスです。 |

**注意点:** NSEスクリプト、特に`intrusive`や`exploit`カテゴリーのものは、ターゲットシステムに悪影響を及ぼす可能性があるため、実行前に必ず内容と影響を理解しておく必要があります。



---

## 8. 実践でよく使うスキャン

### 初回スキャン

```bash
nmap -Pn -sC -sV TARGET
```

### 全ポート

```bash
nmap -Pn -p- TARGET
```

### 全ポート＋詳細

```bash
nmap -Pn -p- -sC -sV TARGET
```

### UDP

```bash
nmap -sU TARGET
```

### OS検出

```bash
nmap -O TARGET
```

---

## 9. 出力の保存

* Normal（`-oN`）
* XML（`-oX`）
* Grepable（`-oG`）
* All Format（`-oA`）

---

## 10. パフォーマンス調整

* Timing（`-T4`）
* Rate
* Host Timeout
* Min Rate

※ 実践では `-T4` を覚えれば十分。

---

## 11. CTF・Pentestで頻出のオプション一覧

| オプション      | 用途         | 使用頻度  |
| ---------- | ---------- | ----- |
| `-Pn`      | Pingを無視    | ★★★★★ |
| `-p-`      | 全ポート       | ★★★★★ |
| `-p`       | ポート指定      | ★★★★★ |
| `-sC`      | デフォルトスクリプト | ★★★★★ |
| `-sV`      | バージョン取得    | ★★★★★ |
| `-O`       | OS検出       | ★★★★☆ |
| `-sU`      | UDP        | ★★★☆☆ |
| `-T4`      | 高速化        | ★★★★☆ |
| `-oA`      | 結果保存       | ★★★★★ |
| `--script` | NSE        | ★★★★☆ |

---

## 12. 攻撃フローでのNmapの使い方

```text
Reconnaissance
    ↓
Nmap (-Pn -sC -sV)
    ↓
全ポート確認 (-p-)
    ↓
Version Detection
    ↓
NSE
    ↓
Enumeration
    ↓
Vulnerability Research
```




